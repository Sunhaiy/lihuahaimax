# ============================================================
# 数据库 (PostgreSQL)
# ============================================================
DATABASE_URL="postgresql://user:password@localhost:5432/lihuahaimax"
# 或拆分写法（pg 连接池优先使用 DATABASE_URL，若无则读以下字段）
PGHOST="localhost"
PGPORT="5432"
PGDATABASE="lihuahaimax"
PGUSER="your_db_user"
PGPASSWORD="your_db_password"

# ============================================================
# NextAuth.js (Auth.js v5)
# ============================================================
# 在生产环境替换为真实随机字符串: openssl rand -base64 32
AUTH_SECRET="your-super-secret-key-replace-in-production"
# 管理员账号（单人博主，写死凭据）
ADMIN_EMAIL="admin@lihuahai.com"
ADMIN_PASSWORD="your-secure-admin-password"

# ============================================================
# 应用基础 URL
# ============================================================
NEXT_PUBLIC_BASE_URL="http://localhost:3000"

# ============================================================
# 媒体文件存储
# ============================================================
# 上传文件保存到服务器本地的绝对路径
UPLOAD_DIR="./public/uploads"
# 对外访问时的 URL 前缀
UPLOAD_PUBLIC_PATH="/uploads"

# ============================================================
# 定时任务 (Cron)
# ============================================================
# 小米手环数据接入预留（未来对接第三方 API 或本地同步脚本）
MI_BAND_SYNC_CRON="0 * * * *"
MI_BAND_API_TOKEN=""
